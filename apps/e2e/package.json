{
  "name": "@planner/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:debug": "playwright test --debug",
    "e2e:headed": "playwright test --headed",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@planner/migrate": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
